import styled from 'styled-components';
import { Text } from 'react-native';

export const Container = styled(Text)`
  font-size: 16px;
  line-height: 32px;
  margin-top: 20px;
  color: #fff;
  max-width: 75%;
`