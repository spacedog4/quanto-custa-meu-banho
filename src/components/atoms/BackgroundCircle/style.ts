import styled from 'styled-components';
import {View} from 'react-native';
export const Container = styled(View)`
  background-color: #20303F1A;
  position: absolute;
  overflow: hidden;
  z-index: -1;
`