import styled from 'styled-components';
import { Text } from 'react-native';

export const Container = styled(Text)`
  font-size: 32px;
  font-weight: bold;
  line-height: 38.4px;
  color: #fff;
  max-width: 75%;
`