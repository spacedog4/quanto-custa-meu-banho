import React from 'react';
import { Container } from './style';

const stopIcon = require('../../../assets/stop-icon.png');

export default function StopIcon() {
    return (
        <Container source={stopIcon}/>
    )
}